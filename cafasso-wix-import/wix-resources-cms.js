(() => {
  // Compatibilidad: la Biblioteca de Recursos ya se alimenta desde el catálogo
  // nativo de CAFASSO en animator-foundation-v1.js. Este archivo se mantiene
  // para no romper referencias antiguas, pero no debe volver a limpiar ni
  // sobrescribir los libros renderizados por CAFASSO.
})();
